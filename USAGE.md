# Kotlin Chinese 使用说明文档

## 📚 目录

- [安装配置](#安装配置)
- [基础类型使用](#基础类型使用)
- [字符串操作](#字符串操作)
- [集合操作](#集合操作)
- [Map操作](#map操作)
- [控制流](#控制流)
- [异常处理](#异常处理)
- [DSL构建器](#dsl构建器)
- [高级用法](#高级用法)
- [最佳实践](#最佳实践)

## 安装配置

### Gradle 配置

在项目的 `settings.gradle.kts` 中确保包含 kotlin-zh 模块：

```kotlin
include(":kotlin-zh")
```

在使用模块的 `build.gradle.kts` 中添加依赖：

```kotlin
dependencies {
    implementation(project(":kotlin-zh"))
}
```

### 导入语句

```kotlin
// 导入所有中文扩展
import com.ohuang.kotlin_zh.stable.*

// 或者按需导入特定功能
import com.ohuang.kotlin_zh.字符串
import com.ohuang.kotlin_zh.stable.列出
import com.ohuang.kotlin_zh.stable.过滤
```

## 基础类型使用

### 类型别名

项目提供了完整的中文类型别名系统：

```kotlin
// 基本数据类型
val 文本: 字符串 = "Hello"
val 整数: 整数 = 42
val 小数: 浮点数 = 3.14f
val 大数: 长整数 = 999999999L
val 开关: 布尔 = true
val 字符: 字符 = 'A'

// 复合类型
val 列表: 列表<字符串> = listOf("a", "b", "c")
val 映射: 映射<字符串, 整数> = mapOf("key" to 1)
val 结果: 结果<字符串> = Result.success("success")

// 通用类型
val 任意对象: 任意 = "anything"
val 可空值: 空 = null
val 无返回值: 无 = Unit
```

## 字符串操作

### 基本字符串方法

```kotlin
val 文本 = "  Kotlin DSL  "

// 检查是否为空或空白
文本.为空或空串()     // false
文本.为空白或空()     // false  
文本.非空白()         // true

// 包含检查
文本.包含("kotlin", 忽略大小写 = true)  // true

// 替换操作
文本.替换全部("kotlin", "Java")  // "  Java DSL  "

// 分割操作
"kotlin,java,python".切分(",")  // ["kotlin", "java", "python"]

// 修剪和转换
文本.去空白()           // "Kotlin DSL"
文本.转小写()           // "  kotlin dsl  "
```

### 字符串构建

```kotlin
// 使用构建字符串函数
val 结果 = 构建字符串 {
    追加行("第一行内容")
    追加行("第二行内容") 
    append("额外内容")
}

// 连接集合为文本
val 列表 = 列出("苹果", "香蕉", "橙子")
列表.连接为文本("、")        // "苹果、香蕉、橙子"
列表.连接为文本("、", "水果：", "")  // "水果：苹果、香蕉、橙子"
```

## 集合操作

### 创建集合

```kotlin
// 不可变列表
val 不可变列表 = 列出(1, 2, 3, 4, 5)

// 可变列表  
val 可变列表 = 可变列出("a", "b", "c")
可变列表 += "d"  // 添加元素

// 不可变集合 (自动去重)
val 不可变集合 = 集(1, 2, 3, 2, 1)  // 结果为 {1, 2, 3}

// 可变集合
val 可变集合 = 可变集("x", "y")
可变集合.添加("z")  // 添加元素
可变集合.添加("x")  // 重复元素不会被添加
```

### 集合查询和判断

```kotlin
val 数字集 = 集(1, 2, 3, 4, 5)

// 检查元素
数字集.包含元素(3)       // true
数字集.不包含元素(10)    // true

// 条件判断
数字集.存在 { it > 4 }        // true (是否有元素大于4)
数字集.所有满足 { it > 0 }    // true (是否所有元素都大于0)
数字集.都不满足 { it > 10 }   // true (是否没有元素大于10)

// 检查是否为空
数字集.为空    // false
集<Int>().为空  // true
```

### 集合运算

```kotlin
val 集合A = 集(1, 2, 3, 4, 5)
val 集合B = 集(3, 4, 5, 6, 7)

// 交集
val 交集 = 集合A.与(集合B)      // {3, 4, 5}

// 并集
val 并集 = 集合A.或(集合B)      // {1, 2, 3, 4, 5, 6, 7}

// 差集
val 差集 = 集合A.差集(集合B)    // {1, 2}
```

### 可变集合操作

```kotlin
val 可变集 = 可变集(1, 2, 3)

// 添加和移除
可变集.添加(4)              // 添加单个元素
可变集.移除(1)              // 移除单个元素

// 批量操作
可变集.添加全部(集(5, 6))   // 批量添加
可变集.移除全部(集(2, 3))   // 批量移除
可变集.保留全部(集(4, 5))   // 只保留指定元素

// 清空
可变集.清空()

// 状态检查
可变集.是否为空     // true (清空后)
可变集.是否非空     // false
```

// 其他集合类型
val 集合 = setOf(1, 2, 3)
val 可变集合 = mutableSetOf("x", "y")
```

### 集合转换

```kotlin
val 数据 = 列出(1, 2, 3, 4, 5, 6)

// 过滤
val 偶数 = 数据.过滤 { it % 2 == 0 }  // [2, 4, 6]

// 映射转换
val 双倍 = 数据.映射 { it * 2 }       // [2, 4, 6, 8, 10, 12]

// 排序
val 排序后 = 数据.排序()              // [1, 2, 3, 4, 5, 6]

// 按条件排序
data class 用户(val 名字: 字符串, val 年龄: 整数)
val 用户列表 = 列出(用户("张三", 25), 用户("李四", 30))
val 按年龄排序 = 用户列表.按排序 { it.年龄 }

// 分组
val 分组结果 = 数据.分组 { if (it % 2 == 0) "偶数" else "奇数" }

// 折叠/归约
val 总和 = 数据.折叠(0) { 累计, 元素 -> 累计 + 元素 }  // 21
```

### 集合属性

```kotlin
val 列表 = 列出(1, 2, 3)

列表.数量          // 3 - 获取元素数量
列表.非空()        // true - 检查是否非空
```

## Map操作

### 创建Map

```kotlin
// 不可变Map
val 基础映射 = 映射构建(
    "姓名" 到值 "阿离",
    "等级" 到值 "7"
)

// 可变Map
val 可变映射 = 可变映射构建("a" 到值 1)
可变映射.放入("b", 2)  // 添加键值对
```

### Map查询

```kotlin
val 映射 = 映射构建("name" 到值 "Alice", "age" 到值 25)

// 基本查询
映射.包含键("name")      // true
映射.包含值("Alice")     // true
映射.取值("name")        // "Alice"
映射.取值或默认("gender", "unknown")  // "unknown"
映射.取值或计算("score") { 100 }      // 100

// 获取键值列表
映射.键列表()            // ["name", "age"]
映射.值列表()            // ["Alice", 25]
映射.条目数             // 2
```

### Map转换

```kotlin
val 原映射 = 映射构建("b" 到值 2, "a" 到值 1, "c" 到值 3)

// 按键/值排序
原映射.按键排序()        // 按key排序的Map
原映射.按值排序()        // 按value排序的Map

// 键值转换
原映射.映射键变换 { 键, _ -> "prefix_$键" }
原映射.映射值变换 { _, 值 -> 值 * 2 }

// 筛选和合并
原映射.筛选项 { _, 值 -> 值 >= 2 }
原映射 合并 映射构建("d" 到值 4)

// 遍历操作
原映射.逐项 { 键, 值 -> 
    打印行("$键: $值")
}
```

### 缓存操作

```kotlin
val 缓存 = 可变映射构建("existing" 到值 1)

// 若无则放入（类似computeIfAbsent）
缓存.若无则放入("new") { 99 }  // 返回99，并添加到map
缓存.若无则放入("existing") { 99 }  // 返回1，不修改
```

## 控制流

### 条件判断

```kotlin
// If-Else结构
如果(条件) {
    // 条件为真时执行
} 否则 {
    // 条件为假时执行
}

// When表达式
val 结果 = 当(数值) { 值 ->
    if (值 > 10) "大于10"
    else if (值 < 0) "负数" 
    else "其他"
}
```

### 循环结构

```kotlin
// While循环
var 计数器 = 0
循环({ 计数器 < 5 }) {
    计数器++
}

// Repeat循环
重复(3) { 索引 ->
    打印行("第 ${索引 + 1} 次迭代")
}

// 中断控制
捕获中断 {
    for (i in 1..10) {
        if (i == 5) 中断()  // 类似break
        打印行(i)
    }
}
```

### 管道操作

```kotlin
// 链式调用管道
val 结果 = "  Hello World  ".管道 { 
    it.trim() 
}.管道 { 
    it.lowercase() 
}

// 空值安全处理
val 名称: String? = "test"
名称.非空时 { 
    打印行("名称不为空: $it") 
}

val 空名称: String? = null
空名称.为空时 {
    打印行("名称为空")
}
```

## 异常处理

### Try-Catch包装

```kotlin
// 基本尝试操作
val 结果 = 尝试 {
    // 可能抛出异常的代码
    riskyOperation()
}

// 成功和失败回调
结果
    .成功时 { 值 -> 
        打印行("成功: $值")
    }
    .失败时 { 异常 -> 
        打印行("失败: ${异常.message}")
    }
```

### 重试机制

```kotlin
// 自动重试
val 最终结果 = 重试(次数 = 3) {
    // 可能失败的操作
    networkCall()
}

// 结合try使用
val 安全结果 = 尝试 {
    重试(次数 = 5) {
        unstableOperation()
    }
}
```

## DSL构建器

### 动态类定义

```kotlin
// 定义一个简单的类
val 狗类 = 定义类 {
    属性("名字", "旺财")
    属性("年龄", 1)
    
    方法("叫") { 
        返回("${取<字符串>("名字")}：汪汪！") 
    }
    
    方法("长大") {
        val 当前年龄 = 取<整数>("年龄")
        设("年龄", 当前年龄 + 1)
    }
}

// 创建实例并使用
val 我的狗 = 狗类.调用类()
打印行(我的狗.调用方法("叫"))  // "旺财：汪汪！"
我的狗.调用方法("长大")
```

### 复杂类示例

```kotlin
val 计算器类 = 定义类 {
    属性("结果", 0.0)
    
    方法("加法") { 参数: Double ->
        val 当前结果 = 取<Double>("结果")
        设("结果", 当前结果 + 参数)
        返回(取<Double>("结果"))
    }
    
    方法("重置") {
        设("结果", 0.0)
        返回("已重置")
    }
}

val 计算器 = 计算器类.调用类()
计算器.调用方法("加法", 5.0)
计算器.调用方法("加法", 3.0)
打印行(计算器.取<Double>("结果"))  // 8.0
```

## 高级用法

### 组合使用示例

```kotlin
fun 数据处理示例() {
    // 原始数据
    val 原始数据 = 列出(" kotlin ", "java", "", "  python  ")
    
    // 清洗和处理
    val 清理后 = 原始数据
        .过滤 { it.非空白() }
        .映射 { it.去空白().转小写() }
    
    // 条件处理
    如果(清理后.非空()) {
        打印行("有效条目: ${清理后.数量}")
        
        // 进一步处理
        val 结果 = 清理后
            .映射 { it.长度() }
            .排序()
            
        打印行("长度分布: ${结果.连接为文本()}")
    }
}
```

### 错误处理模式

```kotlin
fun 安全网络请求(): 结果<字符串> {
    return 尝试 {
        重试(次数 = 3) {
            // 模拟网络请求
            if (Math.random() > 0.7) {
                error("网络超时")
            }
            "请求成功"
        }
    }
}

// 使用示例
安全网络请求()
    .成功时 { 数据 -> 
        打印行("获取数据: $数据")
    }
    .失败时 { 错误 -> 
        打印行("请求失败: ${错误.message}")
    }
```

## 最佳实践

### 1. 命名规范
- 保持中文命名的清晰性和一致性
- 避免过长的中文变量名
- 在团队中统一使用标准

### 2. 性能考虑
- 中文扩展函数通常只是包装原生Kotlin函数，性能开销很小
- 对于高频调用的场景，可以考虑直接使用原生API

### 3. 混合使用
- 可以在同一项目中混合使用中文和英文API
- 根据团队成员的偏好选择合适的风格

### 4. 文档注释
- 为复杂的中文DSL添加适当的注释
- 记录特殊的使用注意事项

### 5. 测试覆盖
- 充分利用现有的单元测试作为参考
- 为新功能编写相应的中文命名测试

## 常见问题

### Q: 中文编码问题如何处理？
A: 确保项目文件使用UTF-8编码，现代IDE都很好地支持Unicode标识符。

### Q: 性能会有影响吗？  
A: 中文扩展大多是inline函数或直接委托给原生API，性能影响可以忽略不计。

### Q: 能否与现有Kotlin代码混用？
A: 完全可以，中文API只是额外的扩展，不影响原有代码。

### Q: 如何贡献新的中文API？
A: 欢迎提交PR，请遵循现有代码风格并添加相应测试。

---

*更多示例请参考 `app/src/main/java/com/ohuang/kotlin_chinese/ChineseDslDemo.kt` 和单元测试文件。*